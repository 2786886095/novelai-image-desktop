## Langbai NovelAI Studio

请根据你的操作系统下载对应安装包：

| 系统 | 安装包 | 安装说明 |
| --- | --- | --- |
| 🪟 **Windows** (x64) | `Langbai-NovelAI-Studio-1.1.1.exe` | 便携版，双击即用，无需安装 |
| 🍎 **macOS** (Intel + Apple 芯片通用) | `Langbai-NovelAI-Studio-1.1.1-universal.dmg` | 拖入「应用程序」；**未签名**，首次打开请右键 →「打开」 |
| 🍎 **macOS**（压缩包，同上通用版） | `Langbai-NovelAI-Studio-1.1.1.zip` | 解压后即为 `.app`，同样需右键「打开」 |
| 🐧 **Linux** (x64) | `Langbai-NovelAI-Studio-1.1.1.AppImage` | `chmod +x` 后直接运行 |
| 🤖 **Android** | `app-release.apk` | 直接安装；未签名，需允许「未知来源」 |
| 📱 **iOS** | `novelai-mobile-unsigned.ipa` | **未签名**，需用 AltStore / Sideloadly 等工具自行侧载 |

> 桌面端与移动端均为 **API-only** 客户端，需自备 NovelAI Persistent API Token。

### v1.1.1 更新内容

- **深色主题稳定性修复（桌面端）**：统一深色模式下的背景、面板、输入框、文字/图标对比、历史侧栏与各工具卡片，修掉之前「灰蒙、低对比、控件看不清」的问题。
- **设置页 Tag/MCP 服务卡片深色修复**：「提示词/补全」里的 Tag/MCP 配置块不再在深色模式下显示成一大块浅灰，提示文字与状态色也跟随深色。
- **小说推文顶部说明不再溢出**：窄窗口/深色下标题副文案与流程提示会自动换行，不再超出卡片边框。
- **批量图生图结果页精简**：进入最终生成/结果页时隐藏上方标题板块、四步步骤卡与流程提示，把空间留给结果列表（导入/参数/提示词三步仍保留完整导航）。
- 本次仅桌面端 UI 结构与样式调整，未改动批量生成 / ZIP 打包 / 失败重试等业务逻辑；移动端与 v1.1.0 一致（仅版本号同步）。

### v1.1.0 更新内容

- **多语言覆盖大幅补全（切换语言不再大面积回退中文/英文）**：在 v1.0.9 多语言底座上补齐了运行时动态文案——移动端新增完整运行时语言包（`zh-CN/zh-TW/en-US/ja-JP/ko-KR` 每种 182 条 key），设置页展开后的正文/按钮/提示也跟随语言；漫画生成器五种语言界面文案（216 条）+ 45 条确认/Toast/状态消息全部本地化；小说推文页面补齐日/韩界面文案与繁中/日/韩运行时状态文案；并修掉移动端模型/模式下拉里残留的中文默认标签。
- **新增独立「语言」设置区**（移动端设置页顶部），切换更直观。
- 已知遗留：Electron 后端部分返回消息（剪映导出、TTS、导入、NAI 请求错误等）仍可能显示中文，已列为下一批本地化目标；不影响主流程使用。

### v1.0.9 更新内容

- **全新多语言界面（桌面端 + 安卓端）**：在「设置 → 外观」新增界面语言切换，支持简体中文、**繁体中文、English、日本語、한국어** 五种语言。本批已完成主导航 / 顶部菜单 / 工具首页 / 设置分类与「外观/安全」设置 / **生成主流程（文生图·图生图·参数·队列·扣费提示）** / 小说推文首屏与导入页的全量翻译。
- **逐语言真实截图走查**：用安卓多语言截图测试（5 语 × 生成页/设置页 × 手机/平板横屏，共 20 张）逐张核对文字截断、按钮漂移、卡片错位与图标缺字，并修复了英文下账号状态用全角括号导致的缺字方块、CJK 图标显示为方块等问题。
- 未知语言配置会自动回落到简体中文，旧配置不受影响。
- 设置页展开后的部分正文、其余工具页与 iOS 真机走查仍在按批推进；已知差异不影响主流程使用。

### v1.0.8 更新内容

- **手机端「设置」页输入框 / 按钮被键盘遮挡的修复**：为设置列表底部加上键盘高度内边距，编辑下方的 Token / API Key 等字段时，输入框和按钮都能滚动到键盘上方，不再被挡住。
- iOS 仍为未签名 IPA（需自行侧载）；提供 Apple 开发者证书（.p12 + 含本机 UDID 的描述文件）后即可接入 CI 签名、像安卓一样直接安装。

### v1.0.7 更新内容

- **修复发布构建打不出桌面 exe**：CI 在多核并行跑测试时，动态加载大模块（`nai.ts`）偶发超过 5 秒默认超时（本地一直正常），导致桌面端打包步骤失败、Release 里缺少 exe。已为测试调高超时并加入重试，发布构建恢复稳定。**应用功能与 v1.0.6 完全相同**（小说推文 → 剪映工具、批量图生图分步向导等），仅构建配置变更。

### v1.0.6 更新内容

- **新增「小说推文」工具（桌面端）**：把小说 / 字幕一键做成竖屏推文视频草稿——导入 TXT/SRT/ASS/LRC → LLM 自动分镜与旁白（未配置转换 API 或被模型拒答时用本地英文 tag 模板兜底，不再整批卡死）→ 复用全局精准参考保持角色一致 → 批量生图（可续跑、单镜重试、按尺寸/步数/模型分组报价）→ edge-tts 配音或导入音频、可按字幕时间码切分一条长音频 → 运镜 / 转场预览 → 一键导出 **剪映 10.9.0 草稿**（锁定 draft 版本、素材复制进草稿目录、导出前 12 项完整性自检）。项目写入磁盘快照，长队列中途关程序后可恢复。
- **批量图生图界面重做为分步向导**：导入 → 参数 → 提示词 → 生成，每步底部有「下一步」与进度提示，导入区改为拖拽式，顶部任务概览指标更清晰。
- **iOS 应用图标**：CI 现会生成 iOS 图标（去除 alpha 通道以符合 App Store 规范）。
- 启动页停留时间微调（约 0.9 秒，转场更从容）。

> 说明：「小说推文」的真实环境硬前置（V4.5 精准参考跨镜一致性、完整真实端到端）见 `docs/TUIWEN_VALIDATION_STATUS.md`；剪映草稿已在剪映专业版 10.9.0.14196 真实打开验证。

### v1.0.5 更新内容

- **手机端发布包改用固定签名密钥**：从此**新版本可直接覆盖安装**，不再需要先卸载。
  - ⚠️ 仅这一次：从旧版升级到 v1.0.5 时，仍需**先卸载旧版、再安装 v1.0.5**（因为旧版用的是每次都变的临时签名）。装上 v1.0.5 之后，以后每次更新都能直接覆盖。
- 桌面端与代码同 v1.0.4（批量图生图编辑卡 / 精准参考 400 修复 / 漫画 Tag 导入 / 启动加速），仅版本号变更。

### v1.0.4 更新内容

- **批量图生图「提示词」改为图文编辑卡**：左侧图片列表 + 右侧大图与编辑区（提示词 / 改图强度 / 单独高级参数），图片按原比例显示不再被裁切，逐张编辑更顺手（与漫画生成器一致）。
- **修复批量图生图带「精准参考」时报错 400「image field references unknown form part」**：精准参考会让请求改用 multipart，此前图生图的原图 / 蒙版仍以 base64 放在 JSON 里、被接口当成分块名 → 报错。现已把原图 / 蒙版作为二进制分块上传并按名引用（桌面 + 手机端同修，含回归测试）。
- **「同步主界面参数」修正全局风格提示词**：批量图生图同步时，全局风格提示词现在取自主界面的「风格提示词」（此前误取正面词）。
- **漫画生成器支持直接导入 Tag 提示词生成**：第 1 步可粘贴或导入 .txt（每行一个分镜的英文 tag），跳过「写故事 → AI 拆分 → 反推」，直接创建分镜即可生成（桌面 + 手机端）。
- **手机端版本更新不再冲突**：发布 APK 改用固定签名密钥（CI 从仓库 Secret 读取），新版本可直接覆盖安装。注意：切换到固定密钥后，本次需先卸载旧版再装一次，之后即可一直覆盖更新。
- **桌面端启动更快**：启动页最短等待从 0.8 秒进一步缩短到 0.3 秒。

### v1.0.3 更新内容

- **启动更快、不再卡死**：手机端启动不再等待账号网络请求——**无代理也能正常打开**；桌面端启动页最短等待从 2.5 秒缩短到 0.8 秒，账号余额改为后台刷新。
- **批量图生图修复（重要）**：修复「开始批量后全部失败且整个界面卡死」。根因有三：缩略图每次重渲染都重新解码整张全分辨率图（O(N²) 解码风暴）、队列异常时卡在「生成中」无法退出、连点导致两次任务互相取消。现已缓存缩略图（稳定解码）、队列异常安全收尾（永不卡在运行态）、并发点击安全（读实时状态）。
- **失效图片自动清理**：在本地（资源管理器 / 文件管理器）删除的图片，图库 / 预览加载失败时直接从应用中移除（桌面与移动端），不再显示破图；移除前会二次确认文件确实不存在。
- **漫画分镜转换**：修复推理模型把额度用在推理上导致「返回被长度截断（内容为空）」——自动以更大额度重试，仍为空才提示改用非推理模型（两端）；新增**「生成未转换分镜」**按钮；转换结束显示**成功 / 失败数量**。
- **手机端自定义图片存放路径**：可在「设置 → 存储与历史」选择文件夹，图片按 **日期 / 分组** 归档（与电脑端一致）；Android 11+ 需授予「所有文件访问权限」，未授权时自动存到应用目录。
- **深度性能优化**：桌面端将漫画 / 批量、局部重绘拆分为**按需加载**（首屏 JS 体积 ~597KB→521KB，冷启动更快）；图库与各网格缩略图改为**懒加载 + 异步解码**；启动时的历史失效清理移出启动关键路径。移动端历史记录改为**内存缓存**（消除每次保存都重复解码整库）+ 大数据 JSON 在**后台 isolate** 解析 / 编码，避免大图库卡顿。
- **批量图生图**：新增更多尺寸预设，缩略图加大显示。

### v1.0.2 更新内容

- **桌面端漫画生成器 / 批量图生图新增「噪声计划（Noise Schedule）」选择**：全局参数、批量逐图参数、漫画逐格覆盖参数均可选择 Native / Karras / Exponential，与主界面一致。（移动端此前已支持。）
- 「同步主界面参数」一键同步本就包含噪声计划在内的全部参数；桌面补上该下拉后，同步结果与主界面完全一致、可见可调。

### v1.0.1 更新内容

- **精准参考改用官方 multipart 上传（关键修复）**：对照官方网页端真实请求（F12 抓包）发现，精准参考图必须以 `multipart/form-data` 的二进制分块（`director_ref_N`）上传，再在 JSON 里用 `director_reference_images_cached` 按 sha256 + 名称引用，`base_caption` 携带类型。此前桌面/移动端把图当 base64 塞进 JSON，接口直接忽略 → 参考图从未生效。现已逐字段对齐官方格式，桌面与移动端同步修复。
- **首个正式包含 Android / iOS 移动端的发布**：移动端基于 Flutter，与桌面共享能力（文生图 / 图生图 / 局部重绘 / 超分 / 后期 / AI 反推与转换 / 漫画 / 批量 / 历史 / 设置 / 新手引导），手机与平板自适应布局。iOS 由 CI 在 macOS 上从同一套代码构建为**未签名 IPA**（需自行侧载）。
- **点击生成立即响应**：桌面端此前在显示「生成中」之前会先串行完成「刷新余额 + 官方报价」两个网络请求，按钮看起来卡住。现改为点击即进入生成态并显示「正在准备生成…」，报价在后台进行（保留取消能力）。
- **默认参数优化**：默认开启 **Variety+（多样化）**，配合质量词提升默认出图质量。
- **精简精准参考**：移除作用不大的「信息提取」滑块（请求仍按官方固定发送）。
- **历史与本地同步**：在本地（资源管理器 / 文件管理器）删除的图片，启动时会自动从应用历史中移除。
- **自适应完善**：修复手机端若干固定宽度弹窗在小屏溢出的问题；布局在 320 宽小屏、横屏手机、手机与平板尺寸下均通过校验。
- 代码清理：移除死代码并开启编译期未使用检查，避免后续堆积。

### v1.0.0 更新内容

- **精准参考重大修复**：修复了精准参考成图被套上整层网点 / 排线纹理的问题。根因是参考类型被错误写入了文本标题字段（`base_caption`），相当于给参考图喂了乱入文本；现已置空，类型改由强度通道表达。同时：RGBA 透明图先**白底拍平**、改用**白边填充**（替代会被当成画面内容的黑边）；新增可调的**「信息提取」**滑块（原先写死最大值）；主界面默认参考类型改为「角色」。**主界面 / 漫画生成器 / 批量图生图三处同步生效**。
- **批量图生图大幅完善**：整个项目存入应用状态，**切换工具 / 标签页不再丢失**已填内容；步骤调整为 导入 → 参数 → 提示词 → 生成；**全局高级参数可调（全模型 / 尺寸 / 采样器 / 步数 / CFG）**+ 全局**精准参考与氛围迁移** + 全局风格/负面（默认取自主界面，可改）；**单张可覆盖高级参数**；新增**清除 / 导出 / 导入项目**；提示词步骤做成漫画分镜式卡片（双击放大、改词后单张重新生成）；生成时**实时显示成图**，支持**重试 / 重新生成（自动删除上一次成图）/ 生成未完成**；打包 ZIP 改为**按名称升序**。
- **灵感胶囊扩充到 4000+ 标签**：14 大类、细分子类，每个标签都归入正确类别，均带中文（独立编写，非 GPL 数据衍生）。
- **图片可拖出**：主预览图、历史缩略图、批量图生图结果卡、漫画分镜成图均可**直接拖出为真实 PNG 文件**到桌面 / 资源管理器 / Photoshop / 聊天窗口等。
- **工作台可拖动分栏**：左 / 中 / 右三栏之间可拖动调整宽度，中间自适应；**自动保存**（下次打开一致）；恢复默认有三种方式——悬停分隔条上的 ⟲ 按钮、双击分隔条、或「设置 → 外观 → 恢复默认分栏宽度」。
- **保留图片元数据开关**（设置 → 存储）：可选择保存的 PNG 是否内嵌生成信息（提示词 / 种子 / 参数）。关闭则导出「干净」图片用于分享（无损，仅删除元数据）；新手向导「保存」步也有说明。
- 界面微调：生成 / 重绘 / 超分 / 后期 / 反推的左右操作栏更宽，全屏时不再大片留白。

### v0.9.9 更新内容

- **新工具「批量图生图」**（工具板块，漫画生成器风格的分步向导：导入 / 提示词 / 参数 / 生成）：
  - 进入先填**分组名称**，导入待处理图片（默认按文件名排序）。
  - 提示词三种来源：**批量文本框粘贴后「导入文本」**（每行一条按顺序对应）、**导入 .txt 文件**、或 **AI 反推填充空缺**（可选「标签 / 自然语言 / 混合」三种反推模式）。
  - **全局改图强度**（默认 0.4）、全局风格 / 负面提示词；模型/尺寸沿用主界面；引入**全图精准参考**；每张可单独改提示词与强度。
  - 串行逐张图生图，结果**存入该分组**（含磁盘 `日期/分组名/` 子目录），完成后**打包 ZIP**。
- **灵感胶囊大改**：「浏览」改用人工校准的**准确分类**（每个标签都属于其类别），并**大量扩充**——14 大类、子分类细化（如「人物」含 对象/身份/年龄/体型/肤色/发型/发色/眼睛/瞳孔/耳朵/口齿/胸部/翅膀尾角 等）；**搜索框**仍可检索完整本地标签库。修复了之前分类标签错配、子分类选中看不清、标签拥挤/超出界面等问题。
- **漫画生成器**：填写分组名后，分镜图片现在真正存入该分组的磁盘子目录（`日期/分组名/`）。
- 杂项：自动补全下拉跟随光标且不再溢出；中→英翻译只翻中文片段并支持「还原」；可开关运行日志（路径可改）；首启引导界面重做；精准参考按官方策略缩放填充并提示推荐尺寸。

### v0.9.8 更新内容

- **本地中文标签库（可选下载）**：在「设置 → 中文标签库」一键下载 Danbooru 中文标签库（约 5 万条，均含中文翻译、带热度），下载后：
  - **提示词补全**：支持中文或英文输入，候选标签均带中文与热度；补全弹窗现**跟随光标出现在正在输入的字下方**，长标签不再被裁切。
  - **灵感胶囊精细分类**：仿专业工具的语义细分——人物 / 服饰 / 表情动作 / 画面 / 环境 / 场景 / 物品 / 镜头特效 / 魔法系，每大类含数个到十余个子类（如人物→对象/身份/年龄/头发/眼睛/瞳孔/耳朵/…），每个子类按**热度**展示几十到上百个真实标签，分类横向滚动、省空间。
- **主生图队列**：主「生成」运行时可折叠的队列面板（运行/排队数、整体进度、逐条状态与移出、清空排队、暂停/继续/停止）；排队数含初始批量剩余；清空可停掉初始批量；清空/取消会让在途报价失效，避免错误入队。
- **中→英翻译增强**：中英混合提示词现在**只翻译其中的中文片段**（英文标签保留）；新增**还原**按钮，一键回到翻译前文本。
- **首次启动引导界面**：功能介绍 + GitHub 项目主页 → 代理 → NovelAI Token → 选填（反推/转换/翻译接口）→ 图片输出目录 → 下载中文标签库 → 界面说明 → 完成；可随时「跳过」，并可在设置中「重新查看新手引导」。
- **错误/运行日志**：新增可开关的「运行日志」，记录生成 / 图生图 / 超分 / 导演工具等调用信息与异常，写入 `app.log`；日志**存放路径可在设置中自定义**，并可一键打开日志文件 / 文件夹。
- **精准参考**：按官方策略，参考图等比缩放并黑边填充到最接近的官方尺寸（1024×1536 / 1472×1472 / 1536×1024）；放入参考图后给出**推荐尺寸**提示。
- **计费更准（依官方文档）**：精准参考按官方「每张生成图固定 5 Anlas」（不再按参考数叠加）且仅 V4.5 计费；Vibe 编码改为**一次性计费**并对**已缓存/同参考排队**的图片不再重复计费。

### v0.9.7 更新内容

- **精准参考图伪影修复（取代 v0.9.6 的黑边填充方案）**：v0.9.6 把参考图缩放并黑边填充到固定 `1024×1536` 虽然消除了 422，但黑边会污染精准参考条件，导致生成图出现斜线 / 网格 / 方格伪影。现改为**原图直传、绝不自动缩放或填充**：选图时与付费请求前都会校验尺寸（宽高均为 64 的整数倍、每边 64–1600），不合规直接拒绝并提示用户先用图片工具调整，避免重采样伪影。*（付费接口改动，请用真实 V4.5 Token 实跑确认。建议优先使用 NovelAI 标准分辨率，如 832×1216 / 1024×1024 / 1216×832。）*
- **主生图队列**：主「生成」任务运行时新增「加入队列」，每点一次把当前提示词 / 模型 / 尺寸 / 采样 / 种子 / Vibe / 精准参考等**完整快照**排队，当前图完成后按顺序串行生成；支持暂停 / 继续 / 停止（停止会中止在途请求并清空队列）。加入前先取该图的生成前 Anlas 报价，报价失败或余额不足则不加入；以运行 ID 防止取消后的旧请求结果污染新任务；仅主生图显示该按钮（图生图 / 重绘 / 超分 / 后期不显示）。
- **新增「按分组保存」**：在历史面板选中某个分组后，新生成的图片**默认归入该分组**，并在磁盘上写入 `输出目录/日期/分组名/` 子文件夹，不同分组的图片分别落到各自的子文件夹（漫画生成仍归其自身项目分组，不受影响；未选具体分组时维持原「日期」目录行为）。

### v0.9.6 更新内容

- **修复精准参考报错 `Error encoding v4 director references: non-200 response: 422`**：此前精准参考图会按长宽比裁切到 `1024×1536` / `1472×1472` / `1536×1024` 三种尺寸之一，但 NovelAI 的 V4.5 director-reference 编码器**只接受唯一分辨率 `1024×1536`**（`1472×1472` 等并非合法输入），方形 / 横图参考因此被服务器编码器拒绝（422）。现改为对齐官方 SDK：参考图**等比缩放后黑边填充到固定 `1024×1536`**（不裁切、不拉伸变形）。
- 同时补回 director-reference 描述中的 `char_captions: []` 字段，与官方 SDK 序列化结果完全一致（此前漏发该键，编码器 schema 校验更易失败）。
- *（付费接口改动，请用真实 V4.5 Token 实跑一张精准参考确认 422 消失。）*

### v0.9.5 更新内容

- **付费请求安全（防重复扣费）**：文生图 / 图生图 / 重绘 / 超分 / Vibe 编码等付费 POST **仅在 429（限流，发生在扣费前）重试，永不在 5xx 重试**；同时移除渲染层的二次重发、漫画「取消」与离开漫画页会中止在途付费请求 —— 杜绝单图最多被发送 8 次重复扣费的风险。
- **报价来源诚实标注**：只有命中 NovelAI 官方报价接口才标注「官方（实际扣费）」；本地前端公式 / 固定规则一律标注「估算（非实际扣费）」，漫画确认弹窗同步措辞。
- **Vibe 编码缓存 + 失败即报错**：相同图像 + 模型 + 提取值的 `encode-vibe` 结果按哈希缓存，避免漫画批量重复编码重复扣费；编码失败时**直接报错**而不再静默改发原图。
- **错误分类更准**：仅当报错确实指向参考字段时才按「参考图错误」无参考重试，避免对尺寸 / 模型 / 提示词错误浪费一次付费重试并掩盖真实原因；余额刷新失败时标注「缓存」。
- **新增「精准参考 Precise Reference」（V4.5）**：主界面参考图弹窗新增精准参考区，可选参考类型（角色 / 风格 / 角色和风格）+ Strength + Fidelity，发送官方 `director_reference_*` 字段，参考图自动裁剪缩放到官方分辨率；漫画参考图按类型自动路由；按 5 Anlas / 张计入生成前报价。*（依赖真实 V4.5 Token 验证。）*
- **安全加固**：NovelAI Token 与视觉 / 转换 / 百度等 AI 密钥使用 Electron `safeStorage` 加密落盘（内存仍为明文，旧明文自动迁移）；自定义 Endpoint 默认**拒绝向非 `*.novelai.net` 地址发送 Token**，可在设置中显式开启。
- **参数校验**：生成尺寸吸附到 64 的整数倍（64–1600），CFG 上限 10，并在主进程二次兜底；「多样化 Variety+」改为发送真实的 `skip_cfg_above_sigma`（此前的 `variety:true` 为空操作）；V4/V4.5 下隐藏无效的 SMEA 开关。
- **历史保留天数现真正生效**：启动时自动清理超过设定天数的应用内历史记录；**仅清理列表，不删除已保存到本地的图片文件**。移除无效的 `deleteProtectionSeconds` 死设置。
- **修复**：重绘 / 图生图请求结构回退到稳定可用版本（图生图保留「原图缩放到目标输出尺寸」修复）。

### v0.9.4 更新内容

- **生成前精准预扣费**：所有生成入口（文生图 / 图生图 / 重绘 / 超分 / 后期 / 漫画队列）在请求前先刷新余额并算出本次预计 Anlas。
  - 文生图优先调用 NovelAI 官方报价接口（`/ai/generate-image/request-price`），失败则回退官网前端同款定价公式；按本应用实际发送的单图请求数相乘。
  - 图生图 / 重绘按实际 strength 计费；重绘按补齐到 64 倍数后的真实发送尺寸计费，重绘用所选 `*-inpainting` 模型。
  - Opus 免费仅在「单张文生图、分辨率 ≤ 1,048,576 像素且步数 ≤ 28」时计入；图生图 / 重绘不算免费。
  - V4/V4.5 Vibe 编码费用计入（每张参考 2 Anlas / 每请求，超过 4 张的额外费用一并计入）。
  - 超分按官方输入像素分档计费（在本应用自动缩放输入后）；Director 工具中去背景固定 65 Anlas、其余当前为免费。
  - **余额不足或无法读取报价时，在请求前直接阻止执行**；生成后仍保留账户余额前后差作为实扣核对。
  - 左侧功能面板实时显示生成前报价并保留上次实扣对照；漫画队列对每种不同分镜配置分别报价、汇总队列总价并在确认弹窗显示，余额不足则拦截。

### v0.9.3 更新内容

- **修复局部重绘 HTTP 400 / 500**：
  - 400 根因是失败兜底时把 `action: "infill"` 发给了不支持 inpaint 的普通模型（如 `nai-diffusion-4-5-curated`）；现移除该错误方向，仅在 `*-inpainting` 模型间兜底（curated inpainting → full inpainting）。默认重绘模型改为 **NAI Diffusion 4.5 Full Inpainting** 并置顶推荐。
  - 500 常见于原图尺寸非 64 倍数。现在重绘前**自动把原图与蒙版同步补边到 64 的整数倍**（原图按边缘像素延展、蒙版补黑色不重绘新增边缘），NovelAI 返回后**自动裁回原始尺寸**再保存，例如 `739×1078` 会以 `768×1088` 发送、保存仍是 `739×1078`，无需手动改图。
  - 补边后仍 500 时给出更可读诊断（已补边尺寸、实际重绘模型）。
- **图生图 / 重绘支持拖入图片**：左侧工作台图片卡片、生成 / 超分 / 后期中央预览区、重绘空白区与已有原图画布均支持直接拖入图片加载；拖入新图自动重置旧蒙版。修复新版 Electron 下 `File.path` 为空导致拖入无效的问题（改用 `webUtils.getPathForFile`）。
- 主进程新增轻量依赖 `pngjs` 用于 PNG 像素级补边 / 裁剪；JPG/WebP 原图先解码为 PNG 再处理。

### v0.9.2 更新内容

- **真正修复「从漫画页生图后回到生成 / 重绘，提示词输入框点不进去」**（v0.9.1 的处理无效，本版替换为根因修复）：
  - 漫画项目本地缓存从「每次状态变化立即同步写 `localStorage`」改为 **700ms 防抖保存**，且保存时不再写入参考图 `previewUrl` 的 data URL 副本（加载时由 `base64` 自动重建），大幅减少主线程上的 JSON 序列化卡顿。
  - 离开漫画生成器时立即标记队列取消并清理保存定时器，异步生图返回后不再继续刷新历史 / 账户或回写已卸载页面状态。
  - 正面提示词不再在每个字符输入时触发设置持久化（旧逻辑持久化又会清空输入，易造成假性「打不进字」）；自动补全在组件卸载时清理定时器。
  - 顶部 Toast 提示浮层加 `pointer-events: none`，不再拦截下方提示词区域的点击。
- **漫画队列健壮性**：生图前校验 Token / Image Endpoint / 提示词；鉴权失败（401/403 归类为 `auth`）立即停止队列而非把后续分镜全刷成失败；带参考图遇 400/422 自动无参考图重试一次；参考图新增「参与最终生图」开关。
- **AI 一致性检测分批**：默认每 6 张一批，单批失败自动二分到单张，最终失败也保留原英文提示词不覆盖。
- **网络 / 游戏 / 动漫角色双版本反推 / 转换**：勾选后返回「角色名版」（如 `furina (genshin impact)`）与「特征版」两套，模型未按 JSON 返回时自动追加一次格式兜底调用。
- **结果查看器**：生成 / 超分 / 后期结果支持滚轮按鼠标位置缩放、放大后中键拖动查看；图生图 / 重绘 / 超分 / 后期完成后保留处理前图并默认开启前后对比滑杆；局部重绘成功后自动载入结果到工作台并重置蒙版。
- **NovelAI 401 处理**：自定义 Image Endpoint 返回 401/403 时自动回退官方 `https://image.novelai.net` 重试一次；仍失败则明确提示重新粘贴并验证 Persistent API Token。

### v0.9.1 更新内容

- **修复漫画批量出图后主界面卡顿**：用漫画生成器出图后切回「生成 / 重绘」，正面提示词输入框一度无响应、过几秒才恢复。原因是批量生成会在每张完成后整段刷新历史记录，导致返回主界面时一次性渲染并解码大量历史缩略图阻塞主线程。现改为整批结束后只刷新一次历史 / 余额，并对历史与漫画缩略图启用懒加载（`loading="lazy"` / `decoding="async"`），输入立即恢复流畅。

### v0.9.0 更新内容

- **局部重绘**：撤回 / 重做改用箭头按钮（↶ / ↷），并把工具栏移到画布上方，绘制蒙版时不再被按钮遮挡。
- **漫画生成器精简为 4 步**：故事与参考图 → 全局设定 → 分镜 → 生成；参考图管理并入第 1 步，移除独立的参考图步骤、返回生图 / 载入示例 / 成人分支 / 上一帧延续等冗余项；批量生成改为逐张串行。
- **新增 AI 一致性检测**：对已转换的分镜英文提示词做跨镜一致性审校（角色 / 服装 / 场景 / 时间线），自动补齐并修正不一致处。
- **AI 反推默认模板**：内置默认替换为「Danbooru 标签｜支持反推范围选择」版（整张 / 角色 / 物品 / 场景）；「恢复默认」改为取真实内置默认，不再把已自定义内容当默认。
- **新增「记录」标签页**：集中查看每次反推 / 转换 / 拆分镜 / 一致性检测发送给 AI 的 system + user 内容与原始返回，可展开 / 复制 / 清空。
- **安全与健壮性修复**：
  - 导入漫画项目 JSON 时剥离外部文件路径，导出 ZIP 仅打包输出目录内、通过图片校验的文件，且改用相对图片名（不再泄露本机绝对路径）。
  - `openExternal` 仅放行 http/https；导入旧 / 残缺项目统一归一化不再崩；队列「自动导出 ZIP」不再漏掉刚生成的图片；本地缓存超额时降级保存并提示。
- **移动端**：修复反推 / 转换输入框光标跳动与 controller 泄漏；版本号同步至 0.9.0。

### v0.8.8 更新内容

- 修复重绘可视画笔：圆形笔刷、圆形准心、遮罩透明度仅影响显示，并新增发送遮罩预览。
- 工具板块改为工具首页，漫画生成器成为第一个工具。
- 漫画生成器新增 API/模板透明展示、目标分镜数量、统一模板模式、新建项目、清空分镜、JSON 导入导出、ZIP 打包。
- 修复长分镜列表无法滚动问题。

### v0.8.6 更新内容

- **局部重绘对标官网（不再出色块 / 不再不按提示词）**：蒙版导出前会**对齐到 64px 方块网格**（与官网可见的「方块蒙版」一致），并强制纯黑白二值化，消除半透明灰边导致的脏色块；画布预览同步显示对齐后的方块，所见即所发。重绘强度默认从 0.55 调到 **1.0**（遮罩区完全按提示词重画，越低越贴近原图），画笔默认 64px 对齐一个方块。
- **漫画生成器重做工作流（不再一坨）**：改为 **5 步向导**——① 导入故事 → ② 全局设定 → ③ 参考图 → ④ 分镜转换 → ⑤ 队列出图，每步只聚焦一件事；高级参数可折叠。
  - 新增**生成队列**：进度条 + 暂停 / 继续 / 取消 + 失败单张重试 + 缩略图墙；顺序生成时上一帧自动作为下一张的低强度氛围参考（即使 React 状态未刷新也能正确链式传递）。
  - 角色 / 场景 / 物品参考图可一键**反推并写入全局设定**，让每个分镜都带上人物设定。
  - 生成前曾显示单张 / 选中 / 全部三档预计 Anlas；此估算已在后续版本废弃，改为按 NovelAI 账户余额前后差显示实扣。

### v0.8.5 更新内容

- **三模式模版彻底不混用**：反推 / 转换现在严格按当前所选模式取模版（自定义覆盖 → 内置默认），**彻底忽略**旧的全局模版字段（`convertSystemPrompt` / `visionSystemPrompt`，无界面、易隐性覆盖当前模式），避免「选了自然语言却被旧模板带偏」。
- **输出格式自纠 + 清理**：请求时附带当前模式的明确输出约束；返回后清理 `Output:` / markdown / 引号 / 换行等包装；若输出明显跑偏（如自然语言模式返回了 tag 列表）会做**一次**低额度（900）修复请求。修复为「尽力而为」——即使启发式仍判定不符也不会硬失败丢弃结果。
- 新增 12 项测试覆盖模版取用、格式检测与清理。

### v0.8.4 更新内容

- **AI 反推 / 转换 报错更清晰 + 兼容推理模型**：实测确认请求格式、三模式模版、视觉反推在 xAI（grok-4.3 等）均正常工作并遵循模版。但默认模型 `gpt-4o` / `gpt-4o-mini` 在非 OpenAI 服务（如 xAI）不存在（返回 400），这是接入第三方服务时「返回为空 / 结果不对」的常见原因。本版：返回为空时明确提示要把模型填成该服务支持的名字（如 grok-4.3，可点检测模型选择），并区分推理模型的长度截断；同时把反推 / 转换输出额度调大（600/900 → 2000）。

### v0.8.3 更新内容

- **重绘预览与其他三个标签对齐**：局部重绘的预览原来用 grid + 视口高度上限 + 棋盘格卡片，显得更小、风格也不一致。现改为与生成/超分/后期同款（绝对定位 flex 居中、图片 `max-height:100%` 同样大小、阴影加在图片上、无卡片）。蒙版画布与图片同宽高比、同 max 约束并居中叠放，像素级对齐，绘制映射不受影响。

### v0.8.2 更新内容

- **彻底修复生成 / 超分 / 后期图片不完整**：经实测定位（画布=651、舞台=601、图片=878），根因是图片舞台用 `display:grid`，其隐式自适应行会被图片内容撑高，导致 `max-height:100%` 失效、竖图溢出被裁。改为 `flex` 居中 + 绝对定位内边距，并给工作区一个确定行高（`minmax(0,1fr)`）让画布有确定高度。复测：图片 601 = 舞台 601，完整不裁。
- **默认提示词模版不再隐藏 + 可一键恢复**：反推 / 转换的内置三模式模版（标签 / 自然语言 / 混合）现在直接显示在设置里，可随时编辑；每个模式带「↺ 恢复默认」按钮随时还原为内置模版；并标注「默认 / 已自定义」。三种模式各用独立系统提示词，**绝不混用**。

### v0.8.1 更新内容

- **彻底修复发现新版本时界面全乱**：v0.8.0 只给第一处 `.app-shell` 加了横幅行，但后面玻璃主题里还有一处 `.app-shell` 用旧的 5 行网格覆盖了它，导致新版本横幅出现时工作区仍然塌陷。本版给这处覆盖规则也补上横幅行（`38px auto 44px 54px 1fr 26px`），无论有无横幅布局都稳定。

### v0.8.0 更新内容

- **修复发现新版本时界面全乱（首次尝试）**：`.app-shell` 网格新增自适应横幅行 + 始终渲染 0 高占位（注：此版未覆盖到玻璃主题里的第二处 `.app-shell`，已在 v0.8.1 补全）。
- **去掉画布底部悬浮工具条**：原本绝对定位在图片底部、遮挡画面——这正是「生成 / 超分 / 后期」图片看起来不完整（而重绘没有该悬浮条所以完整）的原因。已彻底移除定位 / 复制 / 发送等悬浮按钮，图片完整显示、视野不再被挡。

### v0.7.9 更新内容

- **修复角色提示词导致无法生成**：清洗角色提示（去空、坐标转数字并 clamp 到 0~1、最多 6 个）；V4/V4.5 正常走结构化 `v4_prompt.char_captions`，仅在 API 返回 400/422 参数错误时回退为 `base prompt | 角色1 | 角色2` 管道写法（不会因 Token/余额/网络/429/5xx 或 V3 误重试）。
- **修复生成 / 超分 / 后期画布图片被裁切**：修正 `.image-stage` 盒模型（border-box + min-0 + 图片 block/auto），padding 不再撑爆容器裁掉图片边缘。
- **失败提示更明确**：单张生成失败时显示「图片生成失败：具体原因」，不再误显示「完成：成功 0 张」；取消生成不再被误标为失败。
- **图生图健壮性**：payload 准备步骤移入 try，准备/读图阶段异常也走统一错误处理。

### v0.7.8 更新内容

- **代理支持（HTTP / SOCKS5）**：设置 › API 新增「代理地址」，解决国内直连 NovelAI 超时。留空＝直连；填 `http://127.0.0.1:7890` 走 HTTP 代理，`socks5://127.0.0.1:10808` 走 SOCKS5；不写协议默认按 `http://` 处理。
- **统一走代理 + 按需放行**：所有出站请求（NovelAI API、AI 反推/转换、MCP/Tag 服务、谷歌/百度翻译、GitHub 更新检查）默认统一走代理，并可在 API 配置里逐项勾选「哪些不走代理」。

### v0.7.7 更新内容

- **图标统一美化**：把界面里花花绿绿的彩色 emoji（🔒🎨🐾💡🌐✨🎲📌🗑🎉 等）全部替换为一套统一设计的线性矢量图标（描边跟随文字颜色与字号），按功能绘制，风格与原有的 ✓ / ✕ / ⚙ 等线性符号一致，整体更干净协调。

### v0.7.6 更新内容

- **动漫 / Furry 模式**：模型上方新增「🎨 动漫模式 / 🐾 Furry 模式」切换（默认动漫），模型下拉只显示对应家族的模型并记住选择。
- **锁定风格 / 负面提示词**：风格提示词与负面提示词支持 🔒 锁定，锁定后跨重启保留，且不会被「重置为默认」或模板套用覆盖。
- **种子固定 / 随机**：种子新增「🎲 随机 / 📌 固定」切换；固定模式每次复用所填数值，随机模式每次生成都换新种子（文生图 / 图生图 / 局部重绘通用）。
- **内置反推 / 转换模版升级**：AI 反推与提示词转换的默认系统提示词替换为作者编写的 NovelAI V4.5 三模式模版（Danbooru 标签 / 自然语言 / 混合），覆盖 V4.5 多角色 `|` 写法、MCP 角色检索、权重与冲突检查。

### v0.7.5 更新内容

- **去掉提示词红色波浪线**：关闭原生拼写检查，英文 tag 不再被标红。
- **文本框右键菜单**：在任意输入框 / 文本域右键，可使用 剪切 / 复制 / 粘贴 / 全选。
- **历史面板可滑动**：右侧图片列表改为明确的滚动容器（带细滚动条、行不再被拉伸），图片多了也能顺畅滑动查看，不再挤成一团。

### v0.7.4 更新内容

- **提示词标准化**：提示词区新增「✨ 标准化」按钮，可一键规整 tag（转小写、半角标点、移除装饰符号【】「」、下划线转空格、换行转逗号、移除常见质量/artist 前缀、移除非 ASCII 字符、去重），并可选**保留 Wildcards 语法**；参考 NovelAI 官方文档。
- **MCP 标签全中文**：灵感胶囊 / MCP 返回的每个英文 tag 都会标注中文——优先本地词库，未命中则一次性批量在线翻译并缓存，既快又不易触发限流。
- **更新检测**：启动时自动检查 GitHub 最新版本，有新版会在顶部横幅提示并提供「前往下载」直达 Release 页。
- **细节修复**：历史卡片每张图的分组下拉改为与卡片对齐的底部栏（修正边距/圆角不齐）；多项构建与类型检查清理。

### v0.7.3 更新内容

- **修复图片重命名**：历史面板点重命名无反应（Electron 不支持 `window.prompt`），改为应用内输入弹窗；分组重命名同样修复。
- **MCP 提速**：缓存 Streamable HTTP 握手（会话 + 工具参数），重复搜索只需一次请求；灵感胶囊防抖提高到 400ms。
- **MCP 标签中文化**：服务返回的标签会用本地词库补上中文标识。
- **本地词库扩充**：灵感胶囊离线词库新增约 120 个概念（发型/身体/服饰/道具/场景/主题）。
- **提词开关**：提示词区新增「💡 提词：开/关」按钮，可直接开关输入补全。

### v0.7.2 更新内容

- **MCP 接入灵感胶囊**：启用 Tag/MCP 服务后，灵感胶囊按中文搜索时会返回 MCP 标签（默认开启）。
- **反推 / 转换可选用 MCP**：新增独立开关，反推与转换可各自选择是否用 MCP 补强（默认关闭）。
- **修复**：转换面板底部错误提示「设置 › AI 反推 / 视觉模型 API」，已更正为「设置 › 转换 API / 文本模型 API」。

### v0.7.1 更新内容

- **翻译引擎可选**：中→英翻译可在设置中切换谷歌翻译（免费）或百度翻译 API（填 APP ID 与密钥）。
- **MCP 传输适配**：Tag 服务新增 MCP 的 Streamable HTTP / SSE / stdio 三种传输，可直连 DanbooruSearchOnline 的 `search_tags`，并在检测时返回真实错误信息。
- **模型可切换**：检测反推 / 转换接口后，新增下拉列表直接选择并切换模型。
- **图片重命名**：历史与素材面板每张图片新增重命名按钮，会同步重命名本地文件。
- **修复**：生成时「停止」按钮在部分环境下显示为空白，已强制为红底白字并修正布局。

### v0.7.0 更新内容

- **灵感胶囊**：可折叠（默认收起为一行），内置约 190 个中文概念词库，支持复合中文查询，一键插入 Danbooru 标签。
- **标签权重微调**：提示词下方按标签 − / ＋ 调整 `{}` / `[]` 权重。
- **图片命名**：生成面板的文件名前缀。
